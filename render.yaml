services:
  - type: web
    name: legacy-sports-app
    runtime: node
    rootDir: render_server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
