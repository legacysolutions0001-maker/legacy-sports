# ============================================
# LEGACY SPORTS — ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in the values.
# Never commit the actual .env file to GitHub.

DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres
PORT=5000
NODE_ENV=development
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change_this_to_a_long_random_string_at_least_32_chars
# CORS_ORIGIN=https://yourdomain.com
# Super Admin auto-seeded: username=bhullar01 / password=Bhullar_01
# Seed trigger: POST /api/auth/seed with body { "masterKey": "<SESSION_SECRET>" }
